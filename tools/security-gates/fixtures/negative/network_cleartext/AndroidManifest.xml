<?xml version="1.0" encoding="utf-8"?>
<!--
Seeded-negative fixture for gates/network_cleartext.py

This fixture is for the network gate. It includes both network problems
that gate checks: the INTERNET permission and cleartext traffic.

allowBackup is explicitly false and the one activity is a correctly
configured launcher, so no unrelated gate should fire.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:label="fixture">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>

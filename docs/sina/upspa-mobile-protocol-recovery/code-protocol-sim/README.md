# UpSPA Protocol Failure Simulator

This directory contains a small executable model for the UpSPA mobile protocol recovery research.

The simulator evaluates how the client should classify operations when the Login Server and three Storage Providers succeed, fail, or time out independently.

## Run

```bash
python3 simulator.py EOF
